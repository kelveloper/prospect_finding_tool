# Backend API base URL
# Server-side fetches (scoreboard, profile pages) use API_URL; the browser
# (feedback posts) uses NEXT_PUBLIC_API_URL. Point both at the FastAPI app.
API_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
